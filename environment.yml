name: swm_env
channels:
  - conda-forge
dependencies:
  - numpy
  - jax
  - pandas
  - matplotlib
  - seaborn
  - jupyter


# GPU install options below: 
# pip install -U "jax[cuda13]"
# pip install -U "jax[rocm7-local]"